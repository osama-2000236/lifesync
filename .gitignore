node_modules/
.env
client/.env.local
*.log
dist/
coverage/
.DS_Store
__pycache__/
*.pyc

# Local dev database (never commit)
*.sqlite
*.sqlite3

# Local model artifacts (large, machine-specific)
bert_best_model_10pct/
model_runtime/.venv/
model_runtime/artifacts/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

.idea

# Local QA execution artifacts
playwright-report/
test-results/
output/qa/playwright/
.auth/
tests/e2e/screenshots/

# Agent runtime + local scratch (never commit — large binaries, DB data, env backups)
.codex-runtime/
tmp/
output/

# Misc personal/docs scratch (move real docs into docs/)
osama/

# secrets — never commit
client_secret_*.json
*.pem

# local scratch artifacts
*.zip
mysql.err
mysql.out
monitor.sh
